# Вирт

Вадим offical любля грязные полотенца.

### TODO:
- Доавбить eslint (чтобы чекал как js так и ts файлы). Конфиг должен быть основной в корне проекта и для каждого пакета он свой, но дополнительно подтягиет правило из корня.
- Добавить форматер, что бы всё было стильно и я не правил код руками.
- Доавбить commit on lint, что бы были нормальные комиты и мы в низ не запутались сразу.
- Развенуть проекты для каждого пакета.
- Дополнить туду лист другим калом.

### Что нажо для запуска этого кала?
- `pnpm` версии `8.6.11` и выше
- `nodejs` версии `18.0.0` и выше
